<Window x:Class="KutuphaneOtomasyon.Views.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Kütüphane Sistemi - Yönetici Paneli"
        Height="800" Width="1400"
        MinHeight="700" MinWidth="1200"
        WindowStartupLocation="CenterScreen"
        Background="#1e1e2e">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#252536">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Logo ve Başlık -->
                <StackPanel Grid.Row="0" Margin="20,25,20,15">
                    <TextBlock Text="📚" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="Kütüphane Sistemi" FontSize="16" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="txtAdminName" FontSize="11" Foreground="#8b8b9e" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    <Border Height="1" Background="#3a3a4e" Margin="0,15,0,0"/>
                </StackPanel>
                
                <!-- Menü -->
                <StackPanel Grid.Row="1" Margin="12,5,12,0">
                    <TextBlock Text="MENÜ" FontSize="10" FontWeight="Bold" Foreground="#6b6b7e" Margin="8,0,0,8"/>
                    
                    <Button x:Name="btnKitaplar" Content="📖  Kitap Yönetimi" Click="Kitaplar_Click"
                            Height="42" HorizontalContentAlignment="Left" Padding="12,0"
                            Background="Transparent" Foreground="#c8c8d8" BorderThickness="0"
                            FontSize="13"/>
                    
                    <Button x:Name="btnUyeler" Content="👥  Üye Yönetimi" Click="Uyeler_Click"
                            Height="42" HorizontalContentAlignment="Left" Padding="12,0"
                            Background="Transparent" Foreground="#c8c8d8" BorderThickness="0"
                            FontSize="13"/>
                    
                    <Button x:Name="btnOdunc" Content="🔄  Ödünç İşlemleri" Click="Odunc_Click"
                            Height="42" HorizontalContentAlignment="Left" Padding="12,0"
                            Background="Transparent" Foreground="#c8c8d8" BorderThickness="0"
                            FontSize="13"/>
                </StackPanel>
                
                <!-- Çıkış -->
                <StackPanel Grid.Row="2" Margin="12,0,12,15">
                    <Button Content="🚪  Çıkış Yap" Click="Logout_Click"
                            Background="#e74c3c" Foreground="White" BorderThickness="0"
                            Height="40" FontSize="13"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Ana İçerik -->
        <Border Grid.Column="1" Margin="0" Background="#1e1e2e">
            <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden"/>
        </Border>
    </Grid>
</Window>
