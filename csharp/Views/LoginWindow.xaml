<Window x:Class="KutuphaneOtomasyon.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Kütüphane Sistemi - Giriş"
        Height="650" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1a1a2e">
    
    <Grid>
        <StackPanel VerticalAlignment="Center" Margin="50">
            
            <!-- Logo -->
            <TextBlock Text="📚" FontSize="60" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            
            <!-- Başlık -->
            <TextBlock Text="Kütüphane Sistemi" 
                       FontSize="28" FontWeight="Bold" 
                       Foreground="White" 
                       HorizontalAlignment="Center"/>
            <TextBlock Text="Hoş Geldiniz" 
                       FontSize="14" 
                       Foreground="#9ca3af" 
                       HorizontalAlignment="Center" 
                       Margin="0,5,0,30"/>
            
            <!-- Form -->
            <Border Background="#2d2d44" CornerRadius="20" Padding="30">
                <StackPanel>
                    <TextBlock Text="🔐 Giriş Yap" 
                               FontSize="18" FontWeight="Bold" 
                               Foreground="White" 
                               HorizontalAlignment="Center" 
                               Margin="0,0,0,25"/>
                    
                    <!-- Kullanıcı Adı -->
                    <TextBlock Text="Kullanıcı Adı" 
                               FontSize="12" FontWeight="SemiBold" 
                               Foreground="#d1d5db"/>
                    <TextBox x:Name="txtUsername" 
                             Style="{StaticResource ModernTextBox}"
                             materialDesign:HintAssist.Hint="Kullanıcı adınız"/>
                    
                    <!-- Şifre -->
                    <TextBlock Text="Şifre" 
                               FontSize="12" FontWeight="SemiBold" 
                               Foreground="#d1d5db"/>
                    <PasswordBox x:Name="txtPassword" 
                                 Style="{StaticResource ModernPasswordBox}"
                                 materialDesign:HintAssist.Hint="Şifreniz"/>
                    
                    <!-- Şifremi Unuttum -->
                    <Button Content="Şifremi Unuttum" 
                            Click="ForgotPassword_Click"
                            HorizontalAlignment="Right"
                            Background="Transparent"
                            Foreground="#60a5fa"
                            BorderThickness="0"
                            Cursor="Hand"
                            Margin="0,0,0,15"/>
                    
                    <!-- Giriş Butonu -->
                    <Button x:Name="btnLogin" 
                            Content="Giriş Yap" 
                            Click="Login_Click"
                            Style="{StaticResource ModernButton}"
                            Background="#3b82f6"/>
                </StackPanel>
            </Border>
            
            <!-- Kayıt Ol -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <TextBlock Text="Hesabınız yok mu? " Foreground="#9ca3af" VerticalAlignment="Center"/>
                <Button Content="Kayıt Ol" 
                        Click="Register_Click"
                        Background="Transparent"
                        Foreground="#3b82f6"
                        BorderThickness="0"
                        FontWeight="Bold"
                        Cursor="Hand"/>
            </StackPanel>
            
            <!-- Bilgi -->
            <TextBlock Text="Admin: admin / admin123" 
                       FontSize="10" 
                       Foreground="#6b7280" 
                       HorizontalAlignment="Center" 
                       Margin="0,15,0,0"/>
        </StackPanel>
    </Grid>
</Window>
