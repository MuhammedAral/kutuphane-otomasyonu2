<Window x:Class="KutuphaneOtomasyon.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Kütüphane Sistemi - Giriş"
        Height="650" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1e1e2e"
        Icon="/Assets/logo.png"
        KeyDown="Window_KeyDown">
    
    <Grid>
        <!-- MemberWindow Tasarım Dili:
             Arka Plan: #1e1e2e
             Kutular: #2d2d44 (CornerRadius 15)
        -->
        
        <Border Background="#2d2d44" CornerRadius="15" Width="380" Padding="30" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.3"/>
            </Border.Effect>
            
            <StackPanel>
                <Image Source="/Assets/logo.png" Width="80" Height="80" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                
                <TextBlock Text="Kütüphane Sistemi" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBlock Text="Lütfen giriş yapın" FontSize="14" Foreground="#9ca3af" HorizontalAlignment="Center" Margin="0,0,0,30"/>
                
                <!-- Kullanıcı Adı -->
                <TextBlock Text="Kullanıcı Adı" Foreground="#c8c8d8" FontSize="13" Margin="0,0,0,8"/>
                <Border Background="#252536" CornerRadius="25" Height="50" Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="👤" VerticalAlignment="Center" Margin="15,0,5,0" FontSize="16" Foreground="#8b8b9e"/>
                        <TextBox x:Name="txtUsername" Grid.Column="1"
                                 materialDesign:HintAssist.Hint="Kullanıcı Adı"
                                 materialDesign:HintAssist.Foreground="#666680"
                                 materialDesign:TextFieldAssist.DecorationVisibility="Hidden"
                                 Background="Transparent" BorderThickness="0"
                                 Foreground="White" FontSize="14"
                                 VerticalContentAlignment="Center" Padding="5,0,15,0"
                                 Height="50" MaxLength="30" CaretBrush="#3b82f6"/>
                    </Grid>
                </Border>
                
                <!-- Şifre -->
                <TextBlock Text="Şifre" Foreground="#c8c8d8" FontSize="13" Margin="0,0,0,8"/>
                <Border Background="#252536" CornerRadius="25" Height="50" Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="🔒" VerticalAlignment="Center" Margin="15,0,5,0" FontSize="16" Foreground="#8b8b9e"/>
                        <PasswordBox x:Name="txtPassword" Grid.Column="1"
                                     materialDesign:HintAssist.Hint="Şifre"
                                     materialDesign:HintAssist.Foreground="#666680"
                                     materialDesign:TextFieldAssist.DecorationVisibility="Hidden"
                                     Background="Transparent" BorderThickness="0"
                                     Foreground="White" FontSize="14"
                                     VerticalContentAlignment="Center" Padding="5,0,15,0"
                                     Height="50" MaxLength="30" CaretBrush="#3b82f6"/>
                    </Grid>
                </Border>
                
                <!-- Şifremi Unuttum -->
                <Button Content="Şifremi Unuttum?" Click="ForgotPassword_Click"
                        HorizontalAlignment="Right" Style="{StaticResource MaterialDesignFlatButton}"
                        Foreground="#60a5fa" FontSize="12" Height="30" Padding="5,0" Margin="0,0,0,20"/>
                
                <!-- Giriş Butonu -->
                <Button x:Name="btnLogin" Content="Giriş Yap" Click="Login_Click"
                        Style="{StaticResource GradientPrimaryButton}"
                        Margin="0,0,0,20"/>
                
                <Separator Background="#3f3f5a" Margin="0,0,0,20"/>
                
                <!-- Kayıt Ol -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="Hesabınız yok mu?" Foreground="#9ca3af" VerticalAlignment="Center" FontSize="13"/>
                    <Button Content="Hemen Kayıt Ol" Click="Register_Click"
                            Style="{StaticResource GradientPrimaryButton}"
                            Height="32" FontSize="12" Margin="10,0,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
