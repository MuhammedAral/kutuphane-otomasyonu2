<Window x:Class="KutuphaneOtomasyon.Views.ForgotPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Şifre Sıfırlama"
        Height="400" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1a1a2e"
        Icon="/Assets/logo.png">
    
    <Grid>
        <StackPanel VerticalAlignment="Center" Margin="40">
            
            <TextBlock Text="🔑 Şifre Sıfırlama" 
                       FontSize="24" FontWeight="Bold" 
                       Foreground="White" 
                       HorizontalAlignment="Center"/>
            <TextBlock Text="Şifrenizi sıfırlamak için bilgilerinizi girin" 
                       FontSize="12" 
                       Foreground="#9ca3af" 
                       HorizontalAlignment="Center" 
                       Margin="0,5,0,25"/>
            
            <Border Background="#2d2d44" CornerRadius="20" Padding="25">
                <StackPanel>
                    
                    <TextBlock Text="Kullanıcı Adı" FontSize="12" FontWeight="SemiBold" Foreground="#d1d5db"/>
                    <TextBox x:Name="txtUsername" Style="{StaticResource ModernTextBox}"
                             materialDesign:HintAssist.Hint="Kullanıcı adınız"/>
                    
                    <TextBlock Text="Yeni Şifre" FontSize="12" FontWeight="SemiBold" Foreground="#d1d5db"/>
                    <PasswordBox x:Name="txtPassword" Style="{StaticResource ModernPasswordBox}"
                                 materialDesign:HintAssist.Hint="Yeni şifreniz"/>
                    
                    <Button Content="Şifreyi Sıfırla" 
                            Click="Reset_Click"
                            Style="{StaticResource ModernButton}"
                            Background="#f59e0b"/>
                </StackPanel>
            </Border>
            
            <Button Content="← Giriş Ekranına Dön" 
                    Click="Back_Click"
                    HorizontalAlignment="Center"
                    Background="Transparent"
                    Foreground="#60a5fa"
                    BorderThickness="0"
                    Margin="0,15,0,0"/>
        </StackPanel>
    </Grid>
</Window>
