<Window x:Class="KutuphaneOtomasyon.Pages.KitapDetayDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kitap Detayları"
        Height="500" Width="550"
        WindowStartupLocation="CenterScreen"
        Background="#1e1e2e"
        Icon="/Assets/logo.png">
    
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Başlık -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock x:Name="txtBaslik" Text="Kitap Adı" FontSize="22" FontWeight="Bold" Foreground="White" TextWrapping="Wrap"/>
            <TextBlock x:Name="txtYazar" Text="Yazar" FontSize="14" Foreground="#8b8b9e"/>
        </StackPanel>
        
        <!-- Detaylar -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Bilgi Kartları -->
                <UniformGrid Columns="3" Margin="0,0,0,20">
                    <Border Background="#252536" CornerRadius="8" Padding="15" Margin="0,0,10,0">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="📅 Yayın Yılı" Foreground="#8b8b9e" FontSize="11" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="txtYil" Text="-" FontSize="20" FontWeight="Bold" Foreground="#3b82f6" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#252536" CornerRadius="8" Padding="15" Margin="0,0,10,0">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="📦 Stok" Foreground="#8b8b9e" FontSize="11" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="txtStok" Text="-" FontSize="20" FontWeight="Bold" Foreground="#10b981" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#252536" CornerRadius="8" Padding="15">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="📖 Ödünçte" Foreground="#8b8b9e" FontSize="11" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="txtOduncte" Text="-" FontSize="20" FontWeight="Bold" Foreground="#f59e0b" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>
                
                <!-- Ek Bilgiler -->
                <Border Background="#252536" CornerRadius="8" Padding="20" Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="ISBN:" Foreground="#8b8b9e" Margin="0,0,15,10"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="txtISBN" Text="-" Foreground="White"/>
                        
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Tür:" Foreground="#8b8b9e" Margin="0,0,15,10"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" x:Name="txtTur" Text="-" Foreground="White"/>
                        
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Raf No:" Foreground="#8b8b9e" Margin="0,0,15,10"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" x:Name="txtRaf" Text="-" Foreground="White"/>
                        
                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Sıra No:" Foreground="#8b8b9e" Margin="0,0,15,0"/>
                        <TextBlock Grid.Row="3" Grid.Column="1" x:Name="txtSira" Text="-" Foreground="White"/>
                    </Grid>
                </Border>
                
                <!-- Son Ödünç Alanlar -->
                <TextBlock Text="📋 Son Ödünç İşlemleri" FontSize="14" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,10"/>
                <Border Background="#252536" CornerRadius="8" Padding="15">
                    <StackPanel>
                        <ItemsControl x:Name="icSonOduncler">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#1e1e2e" CornerRadius="6" Padding="10,8" Margin="0,0,0,5">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding AdSoyad}" Foreground="#c8c8d8"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Tarih}" Foreground="#8b8b9e" Margin="0,0,15,0"/>
                                            <TextBlock Grid.Column="2" Text="{Binding Durum}" Foreground="{Binding DurumRenk}"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock x:Name="txtNoData" Text="Henüz ödünç işlemi yok" Foreground="#8b8b9e" 
                                   HorizontalAlignment="Center" Visibility="Collapsed"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
        
        <!-- Butonlar -->
        <Button Grid.Row="2" Content="Kapat" Click="Kapat_Click"
                Background="#374151" Foreground="White" 
                Height="40" FontSize="13" BorderThickness="0"
                HorizontalAlignment="Right" Padding="30,0" Margin="0,20,0,0"/>
    </Grid>
</Window>
