<Window x:Class="KutuphaneOtomasyon.Pages.OduncDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Yeni Ödünç İşlemi"
        Height="500" Width="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1e1e2e"
        Icon="/Assets/logo.png">
    
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="🔄 Yeni Ödünç İşlemi" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,15"/>
            
            <!-- Kitap Arama -->
            <TextBlock Text="Kitap Ara *" Foreground="#8b8b9e" FontSize="11" Margin="0,0,0,3"/>
            
            <Grid Margin="0,0,0,3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox x:Name="txtKitapAra" Grid.Column="0" Height="32" Background="#252536" Foreground="White" 
                         BorderBrush="#3a3a4e" BorderThickness="1" Padding="8,6" FontSize="12"
                         TextChanged="KitapAra_TextChanged" />
                         
                <Button Grid.Column="1" Click="BarkodTara_Click"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Background="#3b82f6" BorderBrush="#3b82f6"
                        Height="32" Width="45" Margin="5,0,0,0" Padding="0"
                        ToolTip="Barkod Tara">
                    <materialDesign:PackIcon Kind="BarcodeScan" Width="20" Height="20" Foreground="White"/>
                </Button>
            </Grid>
            
            <ListBox x:Name="lstKitaplar" Height="100" Background="#252536"
                     BorderBrush="#3a3a4e" BorderThickness="1" Margin="0,0,0,10"
                     SelectionChanged="Kitap_Selected">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding baslik}" Foreground="#FFFFFF" FontSize="13" Padding="4,2"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#FFFFFF"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#3b82f6"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
            
            <!-- Üye Arama -->
            <TextBlock Text="Üye Ara *" Foreground="#8b8b9e" FontSize="11" Margin="0,0,0,3"/>
            <TextBox x:Name="txtUyeAra" Height="32" Background="#252536" Foreground="White" 
                     BorderBrush="#3a3a4e" BorderThickness="1" Padding="8,6" FontSize="12" Margin="0,0,0,3"
                     TextChanged="UyeAra_TextChanged"/>
            <ListBox x:Name="lstUyeler" Height="100" Background="#252536"
                     BorderBrush="#3a3a4e" BorderThickness="1" Margin="0,0,0,10"
                     SelectionChanged="Uye_Selected">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding adsoyad}" Foreground="#FFFFFF" FontSize="13" Padding="4,2"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#FFFFFF"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#3b82f6"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
            
            <!-- İade Süresi -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="İade Süresi (Gün)" Foreground="#8b8b9e" FontSize="11" Margin="0,0,0,3"/>
                    <TextBox x:Name="txtGun" Text="14" Height="32" Width="80" HorizontalAlignment="Left"
                             Background="#252536" Foreground="White" BorderBrush="#3a3a4e" 
                             BorderThickness="1" Padding="8,6" FontSize="12"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Button Content="İptal" Click="Iptal_Click" 
                            Style="{StaticResource GradientCancelButton}"
                            Width="90" Margin="0,0,8,0"/>
                    <Button Content="Ödünç Ver" Click="Kaydet_Click" 
                            Style="{StaticResource GradientPrimaryButton}"
                            Width="120"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>
</Window>
