<Window x:Class="KutuphaneOtomasyon.Pages.OduncDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Yeni Ödünç İşlemi"
        Height="320" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1e1e2e">
    
    <Grid Margin="25">
        <StackPanel>
            <TextBlock Text="🔄 Yeni Ödünç İşlemi" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,20"/>
            
            <TextBlock Text="Kitap *" Foreground="#8b8b9e" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <ComboBox x:Name="cmbKitap" Height="42" Margin="0,0,0,10" Background="#252536" Foreground="White"
                      DisplayMemberPath="Baslik" SelectedValuePath="KitapID"/>
            
            <TextBlock Text="Üye *" Foreground="#8b8b9e" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <ComboBox x:Name="cmbUye" Height="42" Margin="0,0,0,10" Background="#252536" Foreground="White"
                      DisplayMemberPath="AdSoyad" SelectedValuePath="KullaniciID"/>
            
            <TextBlock Text="İade Süresi (Gün)" Foreground="#8b8b9e" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="txtGun" Text="14" Style="{StaticResource ModernTextBox}"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button Content="İptal" Click="Iptal_Click" 
                        Background="#6b6b7e" Foreground="White" BorderThickness="0"
                        Width="90" Height="38" FontSize="13" Margin="0,0,10,0"/>
                <Button Content="Ödünç Ver" Click="Kaydet_Click" 
                        Background="#3498db" Foreground="White" BorderThickness="0"
                        Width="100" Height="38" FontSize="13"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
