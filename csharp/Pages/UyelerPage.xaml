<Page x:Class="KutuphaneOtomasyon.Pages.UyelerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="Üye Yönetimi"
      Background="#1e1e2e">
    
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="👥 Üye Yönetimi" FontSize="22" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
            <Button Content="+ Yeni Üye" Click="YeniUye_Click" 
                    Background="#27ae60" Foreground="White" BorderThickness="0"
                    Margin="20,0,0,0" Height="36" Padding="16,0" FontSize="12"/>
        </StackPanel>
        
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox x:Name="txtSearch" Grid.Column="0"
                     Style="{StaticResource ModernTextBox}"
                     materialDesign:HintAssist.Hint="Ad soyad veya kullanıcı adı ara..."
                     KeyDown="Search_KeyDown"/>
            <Button Grid.Column="1" Content="Ara" Click="Search_Click"
                    Background="#3498db" Foreground="White" BorderThickness="0"
                    Margin="10,0,0,0" Height="45" Width="80" FontSize="13"/>
        </Grid>
        
        <Border Grid.Row="2" Background="#252536" CornerRadius="8" Padding="1">
            <DataGrid x:Name="dgUyeler" AutoGenerateColumns="False"
                      Background="#252536" Foreground="#c8c8d8"
                      GridLinesVisibility="Horizontal"
                      BorderThickness="0"
                      CanUserAddRows="False" IsReadOnly="True"
                      RowBackground="#252536"
                      AlternatingRowBackground="#2a2a3e"
                      FontSize="13">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#1e1e2e"/>
                        <Setter Property="Foreground" Value="#8b8b9e"/>
                        <Setter Property="Padding" Value="10,12"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="BorderThickness" Value="0"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Foreground" Value="#c8c8d8"/>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding KullaniciID}" Width="50"/>
                    <DataGridTextColumn Header="Kullanıcı Adı" Binding="{Binding KullaniciAdi}" Width="120"/>
                    <DataGridTextColumn Header="Ad Soyad" Binding="{Binding AdSoyad}" Width="*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="180"/>
                    <DataGridTextColumn Header="Telefon" Binding="{Binding Telefon}" Width="110"/>
                    <DataGridTextColumn Header="Rol" Binding="{Binding Rol}" Width="80"/>
                    <DataGridTemplateColumn Header="İşlem" Width="70">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="🗑️" Click="Sil_Click" 
                                        ToolTip="Sil" Width="35" Height="28"
                                        Background="#e74c3c" BorderThickness="0"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
