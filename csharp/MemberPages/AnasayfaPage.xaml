<Page x:Class="KutuphaneOtomasyon.MemberPages.AnasayfaPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Ana Sayfa"
      Background="Transparent">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock x:Name="txtWelcome" FontSize="28" FontWeight="Bold" Foreground="White" Margin="0,0,0,30"/>
        
        <!-- İstatistikler -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,30">
            <Border x:Name="cardOdunc" Background="#3b82f6" CornerRadius="15" Width="200" Margin="0,0,15,0">
                <StackPanel Margin="20">
                    <TextBlock Text="📖 Elimdeki Kitaplar" Foreground="White" FontSize="13"/>
                    <TextBlock x:Name="txtOdunc" Text="0" Foreground="White" FontSize="36" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            
            <Border Background="#ef4444" CornerRadius="15" Width="200" Margin="0,0,15,0">
                <StackPanel Margin="20">
                    <TextBlock Text="⚠️ Geciken" Foreground="White" FontSize="13"/>
                    <TextBlock x:Name="txtGeciken" Text="0" Foreground="White" FontSize="36" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            
            <Border Background="#10b981" CornerRadius="15" Width="200">
                <StackPanel Margin="20">
                    <TextBlock Text="📚 Toplam Ödünç" Foreground="White" FontSize="13"/>
                    <TextBlock x:Name="txtToplam" Text="0" Foreground="White" FontSize="36" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </StackPanel>
        
        <StackPanel Grid.Row="2">
            <TextBlock Text="📋 Son Ödünç İşlemlerim" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>
            <Border Background="#1f1f1f" CornerRadius="10">
                <DataGrid x:Name="dgSon" AutoGenerateColumns="False" Height="250"
                          Background="Transparent" Foreground="White"
                          CanUserAddRows="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Kitap" Binding="{Binding Baslik}" Width="*"/>
                        <DataGridTextColumn Header="Ödünç Tarihi" Binding="{Binding OduncTarihi, StringFormat=dd/MM/yyyy}" Width="120"/>
                        <DataGridTextColumn Header="İade Tarihi" Binding="{Binding BeklenenIadeTarihi, StringFormat=dd/MM/yyyy}" Width="120"/>
                        <DataGridTextColumn Header="Durum" Binding="{Binding DurumText}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </StackPanel>
    </Grid>
</Page>
